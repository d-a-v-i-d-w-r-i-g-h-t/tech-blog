{{#unless homepage}}
  <div id="posts-header" class="mb-2">

    <h3 class="mb-0">
      {{#if dashboard}}
        My Posts
      {{else if homepage}}
      {{!-- empty --}}
      {{else}}
        {{#if posts.length}}
          All Posts by {{username}}
        {{else}}
          No posts available from {{username}}
        {{/if}}
      {{/if}}
    </h3>

    {{#if dashboard}}
      {{!-- NEW POST BUTTON --}}
      <div id="new-post-button" class="fade-in-out display-none">
        <button type="button"
          class="btn btn-primary py-0 new-post-button"
          disabled="true"
        >New Post</button>
      </div>
    {{/if}}

  </div>
{{/unless}}

<div id="all-posts">
  {{#if posts.length}}
    {{#each posts as |post|}}
      {{> postcard post=post loggedIn=../loggedIn homepage=../homepage username=../username dashboard=../dashboard}}
    {{/each}}
  {{else}}
    No posts yet...
  {{/if}}
</div>

{{#unless homepage}}
  <div class="pt-4">
    {{#if dashboard}}
      <p><a class="bg-link" href="/dashboard/comments">See all my comments</a></p>
    {{else}}
      <p><a class="bg-link" href="/comments/{{username}}">See all comments by {{username}}</a></p>
    {{/if}}
  </div>
{{/unless}}
